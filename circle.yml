machine:
  node:
    version: 8.8.0
  timezone:
    America/Detroit
  environment:
    YARN_VERSION: 1.3.2
    PATH: "${PATH}:${HOME}/.yarn/bin:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
    NODE_ENV: testing

dependencies:
  pre:
    # Install yarn
    - |
      if [[ ! -e ~/.yarn/bin/yarn || $(yarn --version) != "${YARN_VERSION}" ]]; then
        curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version $YARN_VERSION
      fi
    # Ensure correct permissions for yarn files
    - sudo mkdir -p ~/.cache/yarn frontend/node_modules backend/node_modules firebase/node_modules test/node_modules
    - sudo chmod -R 777 ~/.cache/yarn frontend/node_modules backend/node_modules firebase/node_modules test/node_modules
    # Increase files that nodemon can watch
    - sudo sysctl fs.inotify.max_user_watches=524288 && sudo sysctl -p
  cache_directories:
    # These directories also contain dependencies
    - ~/.yarn
    - ~/.cache/yarn
    - frontend/node_modules
    - backend/node_modules
    - firebase/node_modules
    - test/node_modules
  override:
    # Use yarn instead of NPM, because it's way faster
    # and because the lockfile ensures consistent builds
    - yarn install --pure-lockfile

test:
  override:
    # Use yarn to run the tests
    - yarn test-all

deployment:
  # Deploy master branch to staging
  staging:
    branch: master
    commands:
      # Deploying to Heroku manually so that we can force push
      # https://circleci.com/docs/continuous-deployment-with-heroku/#pre-or-post
      - |
        cat >~/.netrc <<EOF
        machine api.heroku.com
          login $HEROKU_EMAIL
          password $HEROKU_TOKEN
        machine git.heroku.com
          login $HEROKU_EMAIL
          password $HEROKU_TOKEN
        EOF
      # Heroku cli complains about permissions without this
      - chmod 600 ~/.netrc
      - "[[ ! -s \"$(git rev-parse --git-dir)/shallow\" ]] || git fetch --unshallow"
      - git push --force git@heroku.com:msu-codes-staging.git $CIRCLE_SHA1:refs/heads/master
  # Deploy production branch to prod
  production:
    branch: production
    commands:
      # Deploying to Heroku manually so that we can force push
      # https://circleci.com/docs/continuous-deployment-with-heroku/#pre-or-post
      - |
        cat >~/.netrc <<EOF
        machine api.heroku.com
          login $HEROKU_EMAIL
          password $HEROKU_TOKEN
        machine git.heroku.com
          login $HEROKU_EMAIL
          password $HEROKU_TOKEN
        EOF
      # Heroku cli complains about permissions without this
      - chmod 600 ~/.netrc
      - "[[ ! -s \"$(git rev-parse --git-dir)/shallow\" ]] || git fetch --unshallow"
      - git push --force git@heroku.com:msu-lansing-codes.git $CIRCLE_SHA1:refs/heads/master
